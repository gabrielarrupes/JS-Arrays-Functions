//padronizando strings com o método map;

let nomes = ["giordan silva", "gustavo braga", "GASPAR Oliveira"]

const nomesPadronizados = nomes.map(nome => nome.toUpperCase());

console.log(nomesPadronizados);